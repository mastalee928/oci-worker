# OCI Worker 零配置性能与抢机优化实施计划

> 本文档是后续代码实施、审查和验收的固定基准。实施时以本文为准，避免因上下文压缩遗漏或改变目标。
>
> 当前只记录方案；未获得明确授权前，不修改业务代码、不提交、不推送。

## 一、目标与不可变约束

1. OCI 开机抢占是核心功能。不能为了页面流畅，把到期的开机请求放进可能延迟数分钟的低并发队列。
2. 用户无需理解或手工配置数据库连接池、任务线程、JVM 内存等参数；安装和升级后自动使用合理默认值。
3. 同时兼顾未知配置的普通用户和当前 4C/24GB 服务器，不能只针对单台高配机器硬编码。
4. 公网 IP 是重要结果，实例创建成功链路仍需完整获取公网 IP，不能用缺失公网 IP 的半成品数据换取表面速度。
5. 不修改实例卡片的结构、内容、位置、分组结构、分组样式或分组外观。
6. 普通页面、登录、本地任务列表不能再因后台抢机任务等待数据库连接 20～30 秒。

## 二、当前代码与 Git 基线

- 远端 `master` 当前包含提交 `9afa1b1e`：开机任务固定最多 4 个同时执行，Hikari 最大连接数仍为 10，连接等待改为 5 秒。
- 本地存在未推送提交 `a7f056b0`：并发数允许配置，但默认仍为 4。
- 实施正式方案时，需要先明确处理本地提交 `a7f056b0`，不能把实验提交误当成最终方案推送。
- 固定并发 4 不作为最终方案保留。

## 三、零配置动态抢机调度

### 3.1 不使用固定低并发

开机任务使用独立的高优先级调度通道，根据 CPU、实时队列等待、JVM 内存和错误反馈自动扩缩，而不是固定为 4 或要求用户配置。

建议自动范围：

| 机器 CPU | 初始并发 | 突发上限 |
| --- | ---: | ---: |
| 1～2 核 | 8 | 16 |
| 4 核 | 16 | 32 |
| 8 核及以上 | 32 | 64 |

运行规则：

- 到期任务排队超过约 1 秒，且 CPU、JVM 内存和数据库状态健康时，逐级提高并发。
- 队列清空、资源压力升高或连续发生限流时，逐步降低并发，避免频繁上下抖动。
- 按租户公平轮询，避免单个租户的大量任务长期占满全部执行位置。
- 已到期的抢机任务优先于普通后台维护任务，但不能持有数据库连接等待 OCI 响应。
- 每个任务仍保留自身的防重入保护，同一任务不能重叠执行造成重复开机。

### 3.2 按错误类型分别重试

`OutOfHostCapacity`：

- 表示区域或可用域临时没有主机容量。
- 严格按用户设置的重试间隔继续尝试；当前前端允许 10～600 秒。
- 不做指数退避，不因普通页面流量额外延迟几分钟。
- 可继续尝试其他符合条件的可用域。

`429 TooManyRequests`：

- 仅对触发 429 的租户、区域或对应 OCI 调用通道指数退避。
- 从几秒开始，最大退避 60 秒；成功或不再返回 429 后逐步恢复。
- 不能因为一个租户被限流而拖慢所有其他租户。

`401 NotAuthenticated`、明确配额不足、不可恢复参数错误：

- 停止对应任务，避免无意义请求。
- 向页面、日志和通知返回明确的租户、区域和修复提示。

## 四、数据库连接生命周期修复

MySQL 服务器没有耗尽。已观察到 MySQL `max_connections=151`、历史峰值约 11，但应用 Hikari 曾出现：

```text
total=10, active=10, idle=0, waiting=12
request timed out after 30000ms
```

这说明需要定位应用连接被借出后没有及时归还的问题，不能只扩大连接池掩盖根因。

所有任务和服务方法必须遵循：

```text
短数据库查询/更新
→ 立即结束事务并归还连接
→ 执行 OCI、Telegram、Future 等外部调用
→ 用新的短数据库操作保存结果
→ 立即归还连接
```

必须检查并拆分所有包含以下行为的事务或连接作用域：

- OCI API 调用
- Telegram/HTTP 通知
- `Future.get` 或其他阻塞等待
- 重试循环
- `Thread.sleep`
- WebSocket 广播前后的长处理

临时启用连接持有诊断，连接借出超过约 15 秒时记录取得连接的代码栈。定位完成后评估是否保留较高阈值的生产诊断。

## 五、减少开机任务数据库访问

当前任务每轮存在重复 `selectById`、更新后再查询等访问。正式修改应做到：

1. 每轮尽量只读取一次任务快照。
2. OCI 调用期间不持有数据库连接或事务。
3. 尝试次数和成功次数使用原子 SQL 更新。
4. 状态更新使用带状态条件的更新，例如只允许 `RUNNING` 状态更新，避免并发覆盖。
5. 成功后只读取返回页面/通知真正需要的数据，避免重复查询同一任务。
6. 后台维护、过期清理和状态修复继续与任务列表接口分离。

## 六、零配置弹性数据库连接池

内置默认配置建议：

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 30
      minimum-idle: 4
      connection-timeout: 5000
      validation-timeout: 3000
      idle-timeout: 300000
```

实际行为：

- 低负载时仅保持少量空闲连接。
- 并发增加时按需创建连接，最多 30 条。
- 高峰结束后，多余空闲连接约 5 分钟后逐步回收。
- 数据库连接获取最多等待 5 秒，不能再固定卡住 30 秒。
- 如果用户已在外部 `application.yml` 明确覆盖相同字段，继续尊重用户配置。
- 默认值放在 JAR 内，普通用户通过系统内 JAR 更新即可生效，不需要修改配置文件。

扩大连接池不是连接生命周期修复的替代品。若仍存在连接长期持有，必须继续修复持有代码。

## 七、JVM 内存自动适配

当前安装脚本和旧部署脚本把 Java 最大堆固定为：

```text
-Xmx256m
```

这会导致 4C/24GB 服务器仍只能使用 256MB Java 堆。安装和升级流程应自动设置合理上限，例如：

| 机器内存 | Java 最大堆建议 |
| --- | ---: |
| 1GB 及以下 | 256MB |
| 2GB | 384MB |
| 4GB | 512MB |
| 8GB 及以上 | 1024MB |

要求：

- 新安装用户自动写入合适的 systemd 启动参数。
- 已安装用户执行升级时也更新 systemd 配置，不能只对新用户生效。
- 更新前备份原服务配置；启动失败时自动回滚。
- 不按 24GB 的固定比例分配超大堆，避免浪费内存；当前 4C/24GB 服务器默认 1GB Java 堆即可。

## 八、页面保护与明确错误

数据库连接获取失败时统一转换为：

```text
HTTP 503
系统当前繁忙，请稍后重试
```

前端要求：

- 保留当前页面框架和已有缓存数据。
- 明确提示服务繁忙，不无限加载、不白屏。
- 允许用户重新刷新请求。
- 不能为了保护页面而把 `OutOfHostCapacity` 抢机任务暂停数分钟。

## 九、已有用户的升级兼容与自动迁移

已有用户升级后必须获得完整优化，不能只有新安装用户生效。项目支持两条升级路径，两者最终结果必须一致：

1. 面板中的“系统更新”。
2. 用户通过 SSH 执行：

```bash
ociworker update
```

### 9.1 JAR 内改动的生效规则

以下内容随新 JAR 更新并重启后直接生效：

- 抢机动态并发和公平调度
- `OutOfHostCapacity`、429、401、配额和参数错误的分类处理
- 数据库连接生命周期修复
- 开机任务数据库访问减少和原子状态更新
- Hikari 内置默认值
- HTTP 503 服务繁忙处理和运行指标

服务器上的 `/opt/oci-worker/application.yml` 必须保留：

- 用户没有填写的新配置项，自动使用新 JAR 的内置默认值。
- 用户明确配置过的同名参数继续优先，升级不能擅自覆盖。
- 不覆盖数据库地址、账号、密码、面板设置、密钥目录或其他用户配置。

### 9.2 SSH `ociworker update` 的升级要求

`ociworker update` 应继续下载最新 `install.sh` 并进入升级模式，完成：

```text
备份旧 JAR 和受管理的 systemd 配置
→ 检测机器 CPU、内存和旧启动参数
→ 下载并校验新 JAR
→ 必要时迁移项目旧默认配置
→ 更新管理命令
→ daemon-reload 并重启服务
→ 验证端口和进程状态
→ 失败时回滚 JAR 与 systemd 配置
```

升级模式必须保留数据库、密钥和 `/opt/oci-worker/application.yml`。

### 9.3 面板内系统更新的升级要求

当前面板更新主要替换 JAR 并重启。正式实施时必须补齐与 `ociworker update` 相同的受控迁移能力：

- 更新前备份旧 JAR。
- 检测并备份项目管理的 systemd 配置或 drop-in。
- 使用与安装脚本相同的 CPU、内存和旧默认识别逻辑。
- 更新后执行 `systemctl daemon-reload` 和服务重启。
- 验证服务存活和端口可访问。
- 启动失败时同时回滚旧 JAR 和 systemd 配置。
- 页面更新和 SSH 更新必须调用同一套迁移规则，避免两条路径产生不同结果。

### 9.4 JVM 参数迁移规则

JVM 启动参数位于 systemd，单纯替换 JAR 无法改变已经启动的 Java 最大堆。因此两条升级路径都必须处理旧 `-Xmx256m`：

- 如果检测到项目旧版生成的默认 `-Xmx256m`，视为可自动迁移的旧默认。
- 根据机器内存计算新值，并以受管理的 systemd drop-in 或安全方式更新。
- 如果用户已经自定义 JVM 参数，不自动覆盖，只记录检测结果。
- 修改前保留可恢复备份；新版本启动失败时恢复原参数。
- 用户手工只替换 JAR 时，JAR 内优化可以生效，但 JVM 内存迁移不会生效；日志中应给出明确提示。

### 9.5 旧配置识别原则

自动迁移只能修改能够确认属于项目生成的旧默认值：

- 缺失的新配置项直接使用 JAR 内置默认值，不需要写回用户配置文件。
- 与项目历史默认值完全一致的受管理 systemd 参数可以备份后迁移。
- 无法判断是否由用户修改的值一律保留。
- 不对用户配置做整文件重写，避免破坏注释、缩进和自定义字段。

## 十、运行监控与自动判断依据

至少记录以下指标：

- Hikari：`active`、`idle`、`total`、`waiting`
- MySQL：`Threads_connected`、`Threads_running`
- 抢机队列：到期任务数、最老任务等待时间、当前并发、动态并发上限
- OCI：`OutOfHostCapacity` 次数、429 次数、每租户退避状态、单次调用耗时
- JVM：堆内存使用率、GC、CPU/系统负载
- 页面：普通 API 响应时间和连接获取失败次数

自动扩缩以实时队列等待和资源压力为依据，不能只凭服务器内存大小猜测。

## 十一、实施顺序

1. 确认 Git 基线并处理未推送实验提交 `a7f056b0`。
2. 增加只读连接持有诊断，定位实际长连接代码路径。
3. 拆分数据库阶段与 OCI/通知/等待阶段，消除长事务或长连接持有。
4. 减少任务循环中的重复数据库查询，改用条件更新和原子计数。
5. 移除固定并发 4，实施高优先级、公平、动态扩缩的抢机调度。
6. 区分容量不足、429、401、配额和参数错误的重试策略。
7. 将 Hikari 改为低空闲、按需增长、最大 30 的零配置默认值。
8. 修改安装、SSH `ociworker update` 和面板内更新流程，使 JVM 内存及受管理默认值对新旧用户都能自动适配。
9. 确保两条升级路径共用迁移、备份、验证和回滚规则，并保护用户外部配置、数据库和密钥。
10. 增加 HTTP 503 服务繁忙处理、运行指标和限频日志。
11. 完整构建、测试、代码审查和真实服务器验证后，再统一提交推送。

## 十二、验收标准

- 4C/24GB 服务器抢机任务正常并发不被固定 4 限制，到期任务不会排队数分钟。
- `OutOfHostCapacity` 按用户设置的间隔继续尝试。
- 429 只影响对应租户/区域，不拖慢其他任务。
- OCI 网络调用期间没有数据库连接被长期持有。
- Hikari `waiting` 正常情况下持续为 0，突发后可快速恢复。
- 登录、任务列表、租户配置、实例管理、VCN、Cloudflare、DNS 和日志页面不再普遍等待几十秒。
- 数据库连接不足时页面在 5 秒内收到明确 503，而不是白屏。
- 低配用户无需配置即可运行；高负载时系统自动扩缩。
- JAR 更新保留用户数据库、密钥和外部配置，并能让新的内置默认值生效。
- 面板内更新与 SSH `ociworker update` 都能让已有用户获得相同优化。
- 项目旧默认 `-Xmx256m` 能在升级时安全迁移；用户自定义 JVM 参数不会被覆盖。
- 更新失败能够同时回滚旧 JAR 和受管理的 systemd 配置。
- 不修改实例卡片或分组的结构、样式和外观。

## 十三、参考依据

- Oracle 官方说明：`Out of host capacity` 表示临时缺少 Always Free Shape，应尝试其他可用域或稍后重试。
  <https://docs.oracle.com/iaas/Content/FreeTier/freetier_topic-Always_Free_Resources.htm>
- Oracle 官方 API 指南：遇到 429 限流时建议指数退避，从几秒开始，最大约 60 秒。
  <https://docs.oracle.com/iaas/Content/API/Concepts/usingapi.htm>
- 社区 OCI A1 容量抢占工具，以周期调用 `LaunchInstance` 等待容量释放。
  <https://github.com/hitrov/oci-arm-host-capacity>
