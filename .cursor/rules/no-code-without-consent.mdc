---
description: 未明确需求且未经用户同意时，禁止擅自改代码
alwaysApply: true
---

# 禁止擅自改代码

## 用户提问题 → 只回答

用户是在 **提问、咨询、了解、吐槽、确认** 时：**只用文字回答**，不查完就改、不顺手修、不「顺便优化」。

- 可以：读代码/日志/文档后 **解释、对比、给建议、列可选方案**。
- 不可以：为此去改任何业务代码或配置（除非用户在同一条或后续消息里 **明确同意改代码**）。

## 硬性门槛（同时满足才可改仓库）

1. **用户已确切描述**：要做什么、改哪块、期望结果或验收方式（不能靠猜测补全需求）。
2. **用户已明确同意 Cursor 可以改代码**：例如「可以改」「去做」「实现吧」「帮我改」；仅讨论、咨询、吐槽、续写上下文 **不算** 同意。

任一不满足 → **只读**：说明、查代码、给方案、列步骤；**不** `Write` / `StrReplace` / `Delete` / 提交 / 推送 / 跑会改文件的脚本。

## 明确禁止

- 因「继续开发/优化」「上次没做完」等模糊话 **自行开工**。
- 用户 **只问问题** 时做任何代码改动（含「顺便修一下」）。
- 用户 **未点名** 的文件或功能，主动扩大范围（重构、顺手优化、加 `.gitignore` 等）。
- 未经要求 **commit / push / 发布**。

## 应先做的事

- 需求不清：用简短问题确认范围，等用户回复。
- 用户只要规则/文档且写明「写规则」：只改其点名的产物（如 `.cursor/rules/`），不动业务代码。

## 允许改代码的示例

- 「租户弹窗账务 Tab 加载失败，帮我修，可以改代码。」
- 「按方案 B 实现，开始改。」

## 不允许的示例

- 「继续开发功能以及优化。」→ 先问要做哪几项，等同意后再动代码。
- 「你在干嘛？」→ 只解释，不改代码。
