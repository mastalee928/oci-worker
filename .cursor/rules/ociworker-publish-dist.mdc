---
description: 开发仓与 OCIworker 对外发布仓分离；两套发布互不影响
alwaysApply: true
---

# 两套仓库、两套发布

| 仓库 | 本地 | 远程 | 发布规则 |
|------|------|------|----------|
| **开发仓** | `H:\cursor\oci-worker` | `mastalee928/oci-worker` | **本仓自己的 CI / Release**（`.github/workflows` 等），**不**适用 `OCIWorker-dist` 规则 |
| **对外发布仓** | `H:\cursor\OCIWorker-dist` | `OCIworker/OCIworker` | 仅适用 `H:\cursor\OCIWorker-dist\.cursor\rules\publish-repo.mdc` |

## 在开发仓工作时

- 开发、提交、push、`latest` / `installer-latest` 等由开发仓 CI 与用户指令决定
- **不要**把发布仓的约束（如 commit 只能写「增加以及优化功能。」、禁止删 tag、禁止提交源码到 OCIworker 等）套用到开发仓

## 何时才动公开仓

**公开仓日常不更新。** 只有用户 **特别说明** 要给 `OCIworker/OCIworker` 做更新/发布时（例如「可以发布」「更新公开仓」「在 OCIworker 发一版」），才：

1. 遵守 `H:\cursor\OCIWorker-dist\.cursor\rules\publish-repo.mdc`
2. 使用 **OCIworker** 账号操作该仓

其余一切（日常开发、开发仓 push/Release、打开 `OCIWorker-dist` 目录、检查更新逻辑讨论等）**默认不碰公开仓**，也 **不适用** `publish-repo.mdc`。

同步时从开发仓 **只读复制** 到 `OCIWorker-dist` 再改；是否允许改开发仓以用户当次指令为准。

## 公开仓硬性禁令（发布前必对照 `publish-repo.mdc`）

用户裁定，写入发布仓规则，同步/发布时 **必须** 遵守：

1. **禁止暴露开发仓（含一切描述字眼）**  
   对外脚本、文档、JAR、Release 说明中不得出现 `mastalee928/oci-worker`、`ui-latest`、`feature/ui-polish`、`OCI_WORKER_UI`、动效/Orbis 渠道、开发仓 curl 示例等（详见 `OCIWorker-dist/.cursor/rules/publish-repo.mdc`「禁令一」）。

2. **公开仓不发布动效版与 Docker 版**  
   仅 `latest` 单 JAR + `installer-latest` 安装脚本；不得建 `ui-latest`、不得附整应用 Docker 分发包（详见发布仓规则「禁令二」）。

## GitHub 账号（`gh` / push）

| 场景 | 使用账号 |
|------|----------|
| 日常开发、`mastalee928/oci-worker` 的 push / CI | **mastalee928**（默认） |
| 对外公开仓 `OCIworker/OCIworker` 的 push / Release | **OCIworker** |

推送前确认 `gh auth status` 的 Active account 与目标远程一致；开发仓 push 失败且报 `Repository not found` 时，常见原因是当前登录成了 `OCIworker`，应 `gh auth switch -u mastalee928` 后再推。
