# OCI Worker 页面与接口性能优化计划

## 1. 目标与范围

本计划用于解决实例管理、WebSSH、页面路由切换、开机任务和租户目录加载过程中出现的长时间转圈、主体空白、白屏和接口响应慢问题。

计划分为六个独立阶段实施。每个阶段必须单独验证，禁止一次性混合修改后再定位回归。

### 不可变约束

- 不修改实例卡片的结构、内容、位置、尺寸和交互入口。
- 不修改实例分组的结构、排序、背景、边框和视觉外观。
- 不以删除业务能力、减少安全校验或隐藏加载错误的方式换取速度。
- WebSSH API 与终端 WebSocket 必须继续执行严格鉴权。
- 缓存必须有主动失效机制和兜底刷新机制，不能产生长期权限或配置不一致。
- 实施前保留当前基线；每一阶段均应可单独回滚。

## 2. 链路目标

当前链路：

```text
点击菜单
→ 旧页面先消失
→ 下载新页面代码
→ 同时加载多个未打开的抽屉组件
→ 每个 API 重复查询数据库鉴权
→ 串行获取全部租户
→ 页面最终显示
```

目标链路：

```text
提前预加载页面代码
→ 点击后立即显示页面框架
→ 只加载当前可见内容
→ 鉴权配置走内存快照
→ 列表接口批量读取
→ 抽屉点击时才按需加载
```

## 3. 实施阶段

### 阶段一：修复实例管理无效加载

状态：`代码完成，待部署页面回归`

主要文件：

- `frontend/src/views/InstanceList.vue`
- 当前证据：`TenantVcnPanel`、`VcnManager`、`StorageManager` 位于页面模板中；组件虽为异步导入，但关闭状态下仍可能被解析、下载或挂载。

实施内容：

- `TenantVcnPanel` 仅在 `vcnVisible === true` 时挂载。
- `VcnManager` 仅在 `vcnManagerOpen === true` 时挂载。
- `StorageManager` 仅在 `storageManagerOpen === true` 时挂载。
- 实例详情抽屉、快捷开机弹窗及其他非首屏浮层按相同原则处理。
- 首屏只保留租户分组列表及当前实际可见内容。
- 抽屉组件必须将“是否挂载”和“是否打开”拆成两个状态，不能机械使用 `v-if="open"`。
- `TenantVcnPanel` 当前会在正式打开前通过组件 `ref` 调用 `loadPanel()`；改造时必须先挂载组件，等待 `nextTick()` 后加载数据，再设置打开状态。
- 关闭浮层时等待关闭动画和现有工作区切换流程完成后，再释放不需要的重型组件。
- 保留悬浮租户卡、桌面/移动端延迟打开、抽屉切换及业务状态所必需的数据。

验收标准：

- 首次进入实例管理时不出现无关的“正在载入云网络”。
- 未点击对应入口时，不请求云网络、存储及详情抽屉的异步代码块。
- 打开、关闭各抽屉的功能和数据保持正常。
- 实例卡片与分组的 DOM 结构和视觉截图无变化。

风险与回滚：

- 风险：条件挂载可能暴露未初始化引用或导致抽屉重复初始化。
- 控制：逐个组件改造；验证首次打开、关闭动画、再次打开、租户切换、移动端和桌面端流程，并保护所有组件 `ref` 调用。
- 回滚：仅回滚对应组件的条件挂载，不影响其他阶段。

### 阶段二：修复 WebSSH 白屏

状态：`待实施`

主要文件：

- `backend/src/main/resources/static/webssh/index.html`
- `backend/src/main/resources/static/webssh/static/`
- `backend/src/main/java/com/ociworker/config/AuthInterceptor.java`
- 当前证据：Xterm CSS、Xterm JS、FitAddon 和 WebLinksAddon 仍直接引用 jsDelivr。

实施内容：

- 将 Xterm、FitAddon、WebLinksAddon 的固定版本资源纳入项目静态文件。
- 删除 WebSSH 页面中的 jsDelivr 引用，确保页面运行不依赖外网 CDN。
- 当前 `.js` 和 `.css` 已在面板 Token 校验前放行，但仍位于站点暂停数据库查询之后；`/webssh/index.html` 仍执行完整鉴权。实施时不得把现状误判为所有静态文件都执行完整 Token 鉴权。
- 仅将 `/webssh/static/**` 和经过确认的本地库文件列入精确静态白名单，使其绕开数据库鉴权查询。
- `/webssh/index.html` 继续验证 Cookie Token、站点状态和 IP/设备黑名单。
- `/webssh-api/**` 和终端 WebSocket 保持严格鉴权。
- 禁止将整个 `/webssh/**` 放到拦截器最前面直接放行，避免绕过站点暂停、黑名单和页面入口鉴权。
- 静态资源放行必须使用明确路径白名单，禁止扩大到其他敏感路径。

验收标准：

- WebSSH 页面资源请求全部来自当前站点。
- 断开外网后，WebSSH 页面仍能加载到终端连接阶段。
- 加载一个 WebSSH 页面时，静态资源不再触发数据库鉴权查询。
- `/webssh/index.html` 在未登录、被暂停或命中黑名单时仍按现有安全语义拒绝访问。
- 未登录用户仍不能调用 WebSSH API 或建立终端 WebSocket。

风险与回滚：

- 风险：本地化资源版本与现有调用方式不兼容；静态放行范围过宽；错误地放行 WebSSH HTML 入口。
- 控制：锁定当前 CDN 使用的精确版本，并逐项核对全局对象和插件 API。
- 回滚：恢复原资源引用或收紧单个路径规则；不得回滚终端 API 的鉴权。

### 阶段三：修复页面切换空白

状态：`待实施`

主要文件：

- `frontend/src/layouts/MainLayout.vue`
- `frontend/src/utils/routePrefetch.ts`
- 路由配置与菜单组件
- 当前证据：`MainLayout.vue` 使用 `mode="out-in"`；项目已有 `prefetchMainRoutesIdle()`，但主布局未确认实际调用。

实施内容：

- 移除会先卸载旧页面的 `out-in` 切换方式，避免新路由代码未就绪时主体为空。
- 为异步路由提供统一、轻量的页面骨架或加载占位。
- 当前菜单已经实现鼠标悬停预加载，`task` 和 `log` 也已有导入器；不得重复创建第二套预加载机制。
- 主布局稳定后调用现有 `prefetchMainRoutesIdle()`，并复用其去重与过期代码块处理。
- 为 WebSSH 补充 Vue 路由页面导入器，但只预加载 `WebSSH.vue`，不得提前创建或加载 iframe。
- 菜单获得键盘焦点时复用现有 `prefetchRouteChunk()`；鼠标悬停逻辑保留。
- 空闲预加载仅覆盖明确的高频页面，不在首屏一次下载全部菜单页面。
- 预加载失败不得阻止正常点击导航，也不得产生未处理异常。
- 删除 `out-in` 不能作为唯一修复；异步路由和页面内部数据加载必须同时有统一骨架或明确占位。

验收标准：

- 页面切换期间主体区域始终有旧页面或统一骨架，不出现整块空白。
- 高频菜单点击后通常无需等待路由代码下载。
- 低带宽下预加载不抢占当前页面关键请求。
- 路由切换、返回和连续点击无竞态错误。

风险与回滚：

- 风险：预加载过多增加首屏流量；切换动画叠加导致短暂重影；预加载 WebSSH 时误触发 iframe。
- 控制：仅预加载高频路由，使用空闲调度并设置去重。
- 回滚：可分别关闭悬停预加载、空闲预加载或切换动画调整。

### 阶段四：减少每个 API 的数据库鉴权查询

状态：`待实施`

主要文件：

- `backend/src/main/java/com/ociworker/config/AuthInterceptor.java`
- `backend/src/main/java/com/ociworker/service/PanelAuthService.java`
- `backend/src/main/java/com/ociworker/service/LoginSecurityService.java`

实施内容：

- 当前账号和密码使用 `sys_config` 类型，暂停状态和黑名单使用 `sys` 类型，且写入口不同；不得只在 `NotificationService` 中增加一个统一缓存。
- 拆分为两个不可变快照：`PanelCredentialSnapshot` 保存账号和密码哈希，`LoginSecuritySnapshot` 保存暂停状态、IP 黑名单和设备黑名单。
- 普通请求分别从两个快照读取鉴权配置，不在每次请求中重复查询数据库。
- `AuthController` 初始化或修改账号、密码成功后，必须立即刷新或清除凭据快照。
- `LoginSecurityService` 修改暂停状态或黑名单成功后，必须立即刷新或清除安全快照。
- 增加约 30 秒的兜底刷新周期；具体时长在实现前结合安全要求确认。
- 并发刷新使用单飞或原子替换，避免缓存击穿和读取半更新状态。
- 数据库或刷新失败时保留最后一个完整有效快照并遵循现有安全语义，不得以空黑名单、默认未暂停或默认账号覆盖有效值，也不得默认放行。

验收标准：

- 热缓存下普通 API 鉴权附加数据库查询由约 4～5 次降为 0 次。
- 修改账号、密码、暂停状态或黑名单后，在主动失效路径上立即生效。
- 并发请求下快照只刷新一次，且不存在部分字段新旧混用。
- 修改账号或密码后旧 Token 立即失效，不得继续有效至兜底刷新周期结束。
- 登录、登出、暂停站点、IP/设备封禁的安全回归测试通过。

风险与回滚：

- 风险：缓存失效遗漏导致旧 Token 或旧安全配置继续生效；数据库异常时错误快照覆盖有效快照。
- 控制：覆盖 `AuthController`、`LoginSecurityService` 及其他实际写入口，统一调用对应快照刷新方法，并保留短周期兜底刷新。
- 回滚：关闭快照读取，恢复原数据库读取路径；保留测试覆盖。

### 阶段五：优化开机任务列表接口

状态：`待实施`

主要文件：

- `backend/src/main/java/com/ociworker/service/TaskSchedulerService.java`
- 对应 Repository、Mapper 和 DTO
- 当前证据：列表读取路径会调用 `repairInconsistentRunningTasks()`；租户信息需进一步在实施时确认批量查询或联表方案。

实施内容：

- 启动恢复前保留一次 `repairInconsistentRunningTasks()`，避免将已达标但仍标记为运行中的旧任务重新调度。
- 将列表请求中的运行任务状态修复和过期任务清理移至后台定时维护流程。
- 列表请求只执行分页读取和必要的数据组装，不承担全量扫描或清理。
- 第一版优先使用 `selectBatchIds()` 批量获取当前页租户，确认需要后再考虑 SQL 联表，消除逐条 `selectById()`。
- 租户名称关键字搜索当前允许先查询匹配租户 ID；该固定查询不是 N+1，可继续保留。
- 后台维护过程必须幂等并使用互斥或单飞控制，禁止维护轮次重叠执行。
- 状态修复会调用任务完成逻辑，必须避免与运行线程更新、通知和任务状态集合产生竞争。
- 过期清理安排在低峰周期执行，并与状态修复分开评估频率。
- 为常用过滤、排序字段检查并补充必要索引；未验证执行计划前不盲目加索引。

验收标准：

- 列表请求不再调用全量状态修复和过期清理。
- 单页租户信息不随记录数产生 N+1 查询。
- 常规列表请求稳定在一至两组核心数据库查询内。
- 状态修复和过期清理仍能由后台任务可靠完成。
- 服务启动恢复时，已达成目标的旧运行任务不会被重新调度。

风险与回滚：

- 风险：维护任务延迟导致短时间展示旧状态；批量查询映射遗漏；后台修复与正在运行的任务并发完成。
- 控制：定义允许的状态最终一致窗口，使用互斥和幂等状态转换，并覆盖无租户、租户已删除等情况。
- 回滚：恢复请求内维护调用或逐条映射，但保持接口返回兼容。

### 阶段六：优化租户与分组加载

状态：`待实施`

主要文件：

- `frontend/src/stores/tenantCatalog.ts`
- 对应租户列表 API
- 当前证据：`PAGE_SIZE = 100`，`fetchAllTenantPages()` 串行请求全部分页后才一次返回；租户与分组已有并行刷新入口。

实施内容：

- 首次请求使用后端已支持的 `size=500`；实现前再次验证后端上限。
- 第一页返回后立即更新可见租户列表，超过 500 条时后台继续补充分页。
- 保持当前已经实现的租户和分组并行请求，不重复创建第二套刷新入口。
- 已有缓存先显示，再静默刷新；刷新开始时不得清空现有列表。
- 分组数量根据当前已缓存租户即时计算，并明确后台补充分页期间的加载状态。
- 使用请求代次或取消机制，避免旧请求覆盖新请求结果。
- 后续分页按租户 ID 去重合并，并保持现有稳定排序。
- 第一页可以立即写入可见状态，但只有全部分页成功后才把完整列表提交给 `appQueryCache`。
- 关键字搜索结果不得写入全局租户目录缓存。
- 静默刷新失败时保留旧列表和旧缓存，只更新错误状态。

验收标准：

- 500 条以内通常一次请求即可显示全部租户。
- 超过 500 条时先展示第一页，不等待全部分页完成。
- 刷新期间现有租户和分组数量不归零。
- 搜索、强制刷新和后台补充分页不会相互覆盖或产生重复记录。
- 不完整的第一页数据不会被标记为完整、长期有效的全量缓存。

风险与回滚：

- 风险：渐进合并产生重复、乱序或旧请求覆盖新结果。
- 控制：按租户 ID 去重、保持稳定排序，为请求设置代次标识，并区分“可见部分结果”和“完整缓存结果”。
- 回滚：恢复全量分页合并策略，保留更大的安全页大小需单独评估。

## 4. 实施顺序与提交原则

实施顺序：

1. 实例管理按需挂载。
2. WebSSH 资源本地化与静态鉴权拆分。
3. 页面路由预加载和空白修复。
4. 全局鉴权缓存。
5. 开机任务接口批量化。
6. 租户目录渐进加载。

前三阶段优先解决用户可见的无关加载、页面空白和 WebSSH 白屏；后三阶段降低接口和数据库延迟。

每一阶段应独立提交，并在提交说明中记录：改动范围、验证结果、已知风险和回滚点。不得把六个阶段压成一个无法定位回归的大提交。

## 5. 全局验收指标

- 进入实例管理时仅加载当前可见内容，不出现未打开功能的加载提示。
- 页面导航期间主体不再完全空白。
- WebSSH 首屏不依赖任何第三方 CDN。
- 热缓存下普通 API 鉴权不产生附加数据库查询。
- 开机任务列表无请求内全量维护和租户 N+1 查询。
- 租户列表可渐进显示，静默刷新不清空已有数据。
- 前端类型检查、生产构建、后端测试和后端打包全部通过。
- 实例卡片和分组外观通过修改前后截图或 DOM/CSS 差异确认保持不变。

## 6. 实施记录

| 阶段 | 状态 | 提交 | 验证结果 | 备注 |
| --- | --- | --- | --- | --- |
| 1. 实例管理按需挂载 | 代码完成，待部署页面回归 | - | 前端类型检查与生产构建通过 | 已拆分挂载/打开状态；未修改实例卡片和分组外观 |
| 2. WebSSH 本地资源与鉴权拆分 | 待实施 | - | - | 动态接口与 WebSocket 保持严格鉴权 |
| 3. 路由预加载与空白修复 | 待实施 | - | - | 控制预加载流量 |
| 4. 鉴权配置内存快照 | 待实施 | - | - | 写入后主动失效，失败时不得默认放行 |
| 5. 开机任务列表批量化 | 待实施 | - | - | 后台维护必须幂等 |
| 6. 租户目录渐进加载 | 待实施 | - | - | 防止请求竞态和重复数据 |

## 7. 已有优化基线

- `dabee203`：已优化前端首屏资源加载。
- `dffec20b`：已修复在线更新恢复等待过久。

后续实施必须基于当前代码复核，不得重复实现、覆盖或回退上述已有优化。

## 8. 实施前代码冲突审查结论

六个优化方向均与当前项目兼容，但实施时必须遵守以下修订结论：

1. 实例抽屉不能直接用打开状态控制挂载；必须处理组件 `ref`、`nextTick()`、关闭动画和工作区切换时序。
2. WebSSH 只允许精确放行静态资源路径；HTML 入口、动态 API 和 WebSocket 继续执行对应安全校验。
3. 项目已有鼠标悬停预加载和部分路由导入器；后续只补充空闲调用、键盘焦点及缺失路由，不重复建设。
4. 鉴权缓存必须按数据类型和写入口拆分为凭据快照与安全快照，所有写路径主动刷新。
5. 开机任务维护必须保留启动修复，并对后台维护增加互斥和幂等保护。
6. 租户渐进显示与完整查询缓存必须分离，避免把第一页误当成完整目录。

开始编码前先以本章节为准复核对应实现，不得仅依据最初的概括性处理步骤机械修改。
